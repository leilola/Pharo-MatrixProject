name: CI

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Setup Pharo
        uses: hpi-swa/setup-pharo@v1
        with:
          version: 12.0 # Assurez-vous que "smalltalk-version" est bien "version"

      - name: Run tests
        run: |
          echo "Running tests in Pharo"

